apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "queen-attack"
version = "1.0"
mainClassName = "QueenAttack"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
